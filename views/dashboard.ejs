<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style.css">
    <title>Dashboard</title>
</head>
<body>
    <h1>Dashboard <%= user.userName %></h1>
    <ul>
        <% terms.forEach( el => { %>
                <li class='termItem' data-id='<%=el._id%>'>
                    <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.termName %></span>
                    <span class='del'> Delete </span>
                </li>
        <% }) %>    
    </ul>
    <form action="/dashboard/createTerm" method='POST'>
        <input type="text" placeholder="Year 2 Term 1" name='termName'>
        <input type="submit">
    </form>
    <a href="/logout">logout</a>
    <script src="js/dashboard.js"></script>
</body>
</html>